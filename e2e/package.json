{
  "name": "nje-e2e",
  "version": "1.0.0",
  "description": "NJE Event Platform — Playwright E2E tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  },
  "dependencies": {
    "axios": "^1.15.2"
  }
}
